services:
  mosters:
    build:
      context: ./mosters
      dockerfile: Dockerfile
    volumes:
      - ./posters:/app/posters
    environment:
      - POSTER_DIR=/app/poster
    ports:
      - 8080:8080